# Open Questions — 2026-04-17

- The 12-D retrieval embedding in `src/zpe_prosody/retrieval.py:31-95` is lane-specific and plausibly novel, but the product-level retrieval claim is blocked. For the license schedule, should it be listed as method novelty while explicitly excluding any claim of proven retrieval capability?
- The lane verdict remains `FAIL` because `PRO-C006` missed badly. For license drafting, should the schedule treat Prosody as a codec/packet-format novelty surface only, with retrieval called out exclusively as an unresolved limit?
